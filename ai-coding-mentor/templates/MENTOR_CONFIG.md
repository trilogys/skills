# PROJECT MENTOR CONFIG

- Default mode: `/normal`
- Default mentor level: `L1`
- Project evidence writes: Enabled
- Global profile updates: Explicit `/profile` or `/monthly` only
- Maximum normal-mode learning targets: 1
- Project state visibility: Decide before committing to Git

## Project Learning Goal

Not set.

## Notes

- Current prompt overrides this file.
- `L0` is pure delivery; `L1` is work first; `L2` is balanced; `L3` is growth first; `L4` is study.
